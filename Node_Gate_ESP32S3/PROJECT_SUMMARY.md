# 📦 โปรเจ็กต์สรุป: Node Gate ESP32S3

## ✅ สำเร็จแล้ว!

โปรเจ็กต์ **Node_Gate_ESP32S3v1.0.0** ได้ถูกสร้างเรียบร้อยแล้ว! 

---

## 📁 ไฟล์ที่สร้างขึ้น

### ไฟล์หลัก
- ✅ **Node_Gate_ESP32S3.ino** - โค้ดหลักของ Gateway
- ✅ **thingProperties.h** - การตั้งค่า Arduino IoT Cloud

### เอกสาร
- ✅ **README.md** - คู่มือการใช้งานโปรเจ็กต์
- ✅ **SETUP_GUIDE.md** - คู่มือการตั้งค่าแบบละเอียดทุกขั้นตอน
- ✅ **CHANGELOG.md** - บันทึกการเปลี่ยนแปลงและ Version History

### ตัวอย่าง
- ✅ **examples/Dashboard_Widgets.md** - คู่มือการออกแบบ Dashboard
- ✅ **examples/Send_Command_Example.ino** - ตัวอย่างการส่งคำสั่งไปยัง Tag

---

## 🎯 ฟีเจอร์หลัก

### 1. รับข้อมูลจาก Tag (ESP-NOW)
- ✅ รับข้อมูลแบบ Real-time จาก Tag_sender v3.0.8
- ✅ รองรับข้อมูล: ระยะทาง (Raw, Reg1, Reg2), GPS, Timestamp
- ✅ ตรวจสอบ Packet Loss
- ✅ รับจากหลาย Tag (Broadcast mode)

### 2. อัปโหลดข้อมูลไป Arduino IoT Cloud
- ✅ อัปเดทข้อมูลทุก 200ms
- ✅ รองรับ CloudLocation สำหรับแสดงบนแผนที่
- ✅ ตัวแปร 18 ตัว (Uplink + Downlink)

### 3. ควบคุมจาก Cloud (Downlink)
- ✅ รับคำสั่ง: START, STOP, GOTO, RETURN, EMERGENCY_STOP
- ✅ ส่งคำสั่งกลับไปยัง Tag ผ่าน ESP-NOW
- ✅ กำหนดเป้าหมาย (Target Lat/Lng)

### 4. ระบบตรวจสอบ
- ✅ แสดงสถานะการเชื่อมต่อ (ESP-NOW + IoT Cloud)
- ✅ Watchdog สำหรับตรวจสอบการรับข้อมูล
- ✅ สถิติ Packet (รับทั้งหมด, สูญหาย)

---

## 📊 ข้อมูลที่ส่งไป Arduino IoT Cloud

### Uplink (Tag → Cloud)
| ตัวแปร | ประเภท | คำอธิบาย |
|--------|--------|----------|
| `tagId` | int | หมายเลข Tag |
| `sequenceNumber` | CloudInt | ลำดับ Packet |
| `distanceA1Raw` | CloudInt | ระยะทาง Raw จาก A1 (cm) |
| `distanceA2Raw` | CloudInt | ระยะทาง Raw จาก A2 (cm) |
| `distanceA3Raw` | CloudInt | ระยะทาง Raw จาก A3 (cm) |
| `distanceA1Reg2` | CloudInt | ระยะทาง Reg2 จาก A1 (cm) |
| `distanceA2Reg2` | CloudInt | ระยะทาง Reg2 จาก A2 (cm) |
| `distanceA3Reg2` | CloudInt | ระยะทาง Reg2 จาก A3 (cm) |
| `position` | CloudLocation | ตำแหน่ง GPS (แผนที่) |
| `latitude` | float | Latitude |
| `longitude` | float | Longitude |
| `totalPackets` | CloudInt | Packet ทั้งหมด |
| `packetsLost` | CloudInt | Packet สูญหาย |

### Downlink (Cloud → Tag)
| ตัวแปร | ประเภท | คำอธิบาย |
|--------|--------|----------|
| `command` | String | คำสั่ง |
| `targetLat` | float | Lat เป้าหมาย |
| `targetLng` | float | Lng เป้าหมาย |
| `emergencyStop` | bool | หยุดฉุกเฉิน |

---

## 🔧 ขั้นตอนถัดไป

### 1. แก้ไขไฟล์ `thingProperties.h`
```cpp
// WiFi Credentials
const char WIFI_SSID[]     = "ชื่อ_WiFi_ของคุณ";
const char WIFI_PASSWORD[] = "รหัส_WiFi_ของคุณ";

// Arduino IoT Cloud Credentials
const char DEVICE_LOGIN_NAME[] = "Device_ID_จาก_Cloud";
const char DEVICE_KEY[]        = "Secret_Key_จาก_Cloud";
```

### 2. ตั้งค่า Arduino IoT Cloud
1. ไปที่ https://create.arduino.cc/iot/
2. สร้าง Thing ใหม่
3. เพิ่มตัวแปรตามตาราง (ดู SETUP_GUIDE.md)
4. คัดลอก Device ID และ Secret Key

### 3. อัปโหลดโค้ด
1. เปิด Arduino IDE
2. เลือก Board: **ESP32S3 Dev Module**
3. ติดตั้ง Libraries:
   - ArduinoIoTCloud
   - Arduino_ConnectionHandler
4. Upload

### 4. อัปเดท TAG_sender
อัปเดท MAC Address ใน `Tag_sender_v3_0_8.ino`:
```cpp
// ใส่ MAC Address ของ Node_Gate (ดูจาก Serial Monitor)
uint8_t NODE_PEER_MAC[6] = { 0xAA, 0xBB, 0xCC, 0xDD, 0xEE, 0xFF };
```

### 5. สร้าง Dashboard
1. ไปที่ https://create.arduino.cc/iot/dashboards
2. สร้าง Dashboard ใหม่
3. เพิ่ม Widgets ตาม **examples/Dashboard_Widgets.md**

---

## ⚠️ หมายเหตุสำคัญ

### การส่งคำสั่งกลับไปยัง Tag
เพื่อให้การส่งคำสั่งกลับทำงานได้อย่างสมบูรณ์ ต้อง:

1. **เพิ่ม Peer ใน OnDataRecv()**:
```cpp
// ใน OnDataRecv(), หลังบรรทัด 85
// บันทึก MAC Address ของ Tag
memcpy(targetTagMAC, recv_info->src_addr, 6);

// เพิ่ม Peer ถ้ายังไม่มี
if (!esp_now_is_peer_exist(targetTagMAC)) {
  esp_now_peer_info_t peerInfo;
  memset(&peerInfo, 0, sizeof(peerInfo));
  memcpy(peerInfo.peer_addr, targetTagMAC, 6);
  peerInfo.channel = ESPNOW_CHANNEL;
  peerInfo.encrypt = false;
  esp_now_add_peer(&peerInfo);
}
```

หรือดูตัวอย่างเต็มใน `examples/Send_Command_Example.ino`

---

## 🎉 การใช้งาน

### Serial Monitor Output
```
=== NODE GATE ESP32S3 v1.0.0 ===
Initializing...
[INFO] Gateway MAC Address: 30:C6:F7:1A:2B:3C
[✓] ESP-NOW Initialized
✓ Initialization Complete
Waiting for data from Tag_sender...

[ESP-NOW] Received from Tag-7 | Seq: 123 | GPS: 13.756331, 100.501762
[Cloud] Updated - Tag: 7, Pos: 13.756331, 100.501762

========== SYSTEM STATUS ==========
Firmware: v1.0.0-Gateway
ESP-NOW: Ready
IoT Cloud: Connected
Total Packets: 456
Packets Lost: 2
Last Data Age: 187 ms
===================================

[Cloud] Command changed to: START
[✓] Command 'START' sent to Tag-7
```

### Arduino IoT Cloud Dashboard
- 🗺️ แผนที่แสดงตำแหน่ง Tag แบบ Real-time
- 📊 Gauges แสดงระยะทางจาก 3 Anchors
- 📈 กราฟประวัติการเคลื่อนที่
- 🎮 ควบคุม Tag ผ่าน Command Input
- 🚨 ปุ่มหยุดฉุกเฉิน

---

## 📚 เอกสารเพิ่มเติม

- **คู่มือการตั้งค่า**: อ่าน `SETUP_GUIDE.md`
- **การออกแบบ Dashboard**: อ่าน `examples/Dashboard_Widgets.md`
- **วิธีส่งคำสั่ง**: อ่าน `examples/Send_Command_Example.ino`
- **Version History**: อ่าน `CHANGELOG.md`

---

## 🐛 Troubleshooting

### ไม่ได้รับข้อมูลจาก Tag?
- ตรวจสอบ ESP-NOW Channel (ต้องเป็น 11)
- ตรวจสอบ MAC Address ใน Tag_sender
- ลดระยะห่าง (< 100m)

### IoT Cloud ไม่เชื่อมต่อ?
- ตรวจสอบ WiFi SSID/Password
- ตรวจสอบ Device ID/Secret Key
- ตรวจสอบว่าสร้างตัวแปรครบใน Thing

### ส่งคำสั่งไม่ได้?
- ตรวจสอบว่าได้รับข้อมูลจาก Tag แล้ว
- เพิ่มโค้ด Peer registration (ดูในหมายเหตุ)
- ตรวจสอบ Serial Monitor

---

## 📞 ติดต่อ

- **Email**: micmark1123456@gmail.com
- **GitHub**: https://github.com/lnw007xyz/project3.0.0

---

## 📝 สรุป

โปรเจ็กต์นี้พร้อมใช้งาน! คุณมี:

✅ โค้ดหลัก Node_Gate_ESP32S3.ino  
✅ การตั้งค่า Arduino IoT Cloud  
✅ เอกสารครบถ้วน  
✅ ตัวอย่างการใช้งาน  
✅ คู่มือแก้ไขปัญหา  

**ขั้นตอนถัดไป**: แก้ไข thingProperties.h → ตั้งค่า IoT Cloud → Upload → Test!

**Happy Tracking! 🚀**
